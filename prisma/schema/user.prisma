model User {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  email     String
  role      Role     @default(MEMBER)

  // API Keys
  replicateApiKey String? // User's personal Replicate API token

  // Relations
  queuedEmails QueuedEmail[]
  projects     Project[]
}

enum Role {
  MEMBER
  ADMIN
}
